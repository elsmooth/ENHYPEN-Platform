const express = require('express');
const app = express();
const port = 3001;

app.get('/', (req, res) => {
  res.send('<h1>ENHYPEN Platform - Operational</h1>');
});

app.listen(port, '127.0.0.1', () => {
  console.log('ENHYPEN Platform is running on http://127.0.0.1:3001');
});

